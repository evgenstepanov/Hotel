import React from 'react';

export default function RoomInfo({ singleRoom }) {
  const {
    name,
    description,
    capacity,
    size,
    price,
    extras,
    breakfast,
    pets,
    images,
  } = singleRoom;

  return <div>ROOM_INFO</div>;
}
